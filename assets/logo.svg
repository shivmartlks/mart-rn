<svg width="196" height="64" viewBox="0 0 196 64" xmlns="http://www.w3.org/2000/svg">
  <!-- Symbol -->
  <g transform="translate(0,8)" fill="currentColor">
    <!-- Basket bars -->
    <rect x="2" y="8" width="10" height="32" rx="5"/>
    <rect x="16" y="4" width="10" height="40" rx="5"/>
    <rect x="30" y="8" width="10" height="32" rx="5"/>

    <!-- Subtle base curve -->
    <path
      d="M0 42c8 6 36 6 44 0"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      opacity="0.85"
    />
  </g>

  <!-- Wordmark (closer to icon) -->
  <text
    x="48"
    y="42"
    font-family="Inter, system-ui, -apple-system"
    font-size="28"
    font-weight="700"
    letter-spacing="-0.01em"
    fill="currentColor"
  >
    Shiv Mart
  </text>
</svg>
